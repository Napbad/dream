package main;

import a;

/* ****************GLOBAL VARIABLES******************* */

// global variable
var int a! = 1;

// const b = 10; // global constant

imt c = 100; // global constant with implicit type


var a1 = 2;
// b1 = 20;
// c1 = 200;

var int a2 = 3;
const int b2 = 30;
const int c2 = 300;

// int type variable
    var int numInt = 10;  // Define a mutable IntegerNode variable and initialize it with value 10.
    imt int constNumInt = 20;  // Define an immutable IntegerNode variable and initialize it with value 20.

    // byte type variable (assuming it's used similar to other IntegerNode types in this language context)
    var byte numByte = 42;  // Define a mutable byte variable and initialize it with value 42.
    imt byte constNumByte = 10;  // Define an immutable byte variable and initialize it with value 10.

    // short type variable
    var short numShort = 300;  // Define a mutable short variable and initialize it with value 300.
    imt short constNumShort = 400;  // Define an immutable short variable and initialize it with value 400.

    // long type variable
    var long numLong = 10000000000;  // Define a mutable long variable and initialize it with a large long value.
    imt long constNumLong = 20000000000;  // Define an immutable long variable and initialize it with another long value.

    // string type variable
    var byte[14] str = "Hello, world!";  // Define a mutable string variable and initialize it with a sample string.
    imt byte[17] constStr = "Immutable string";  // Define an immutable string variable and initialize it with another string.

    // float type variable
    var float numFloat = 9.99;  // Define a mutable floating-point variable and initialize it with value 9.99.
    imt float constNumFloat = 8.88;  // Define an immutable floating-point variable and initialize it with value 8.88.

    // bool type variable
    var bool isTrue = true;  // Define a mutable boolean variable and set it to true.
    imt bool constIsTrue = false;  // Define an immutable boolean variable and set it to false.

    // uint type variable (unsigned IntegerNode)
    var uint numUint = 50;  // Define a mutable unsigned IntegerNode variable and initialize it with value 50.
    imt uint constNumUint = 60;  // Define an immutable unsigned IntegerNode variable and initialize it with value 60.

    // ushort type variable (unsigned short)
    var ushort numUShort = 100;  // Define a mutable unsigned short variable and initialize it with value 100.
    imt ushort constNumUShort = 200;  // Define an immutable unsigned short variable and initialize it with value 200.

    // ulong type variable (unsigned long)
    var ulong numULong = 10000000000;  // Define a mutable unsigned long variable and initialize it with a large unsigned long value.
    imt ulong constNumULong = 20000000000;  // Define an immutable unsigned long variable and initialize it with another unsigned long value.

    // llong type variable (long long, assuming it's a larger IntegerNode type)
    var llong numLLong = 12345678901234567890;  // Define a mutable long long variable and initialize it with a very large value.
    imt llong constNumLLong = 98765432109876543210;  // Define an immutable long long variable and initialize it with another large value.

    // ullong type variable (unsigned long long)
    var ullong numULLong = 12345678901234567890;  // Define a mutable unsigned long long variable and initialize it with a very large unsigned value.
    imt ullong constNumULLong = 98765432109876543210;  // Define an immutable unsigned long long variable and initialize it with another large unsigned value.

    // double type variable
    var double numDouble = 199.99;  // Define a mutable double precision floating-point variable and initialize it with value 199.99.
    imt double constNumDouble = 188.88;  // Define an immutable double precision floating-point variable and initialize it with value 188.88.

/* ****************GLOBAL VARIABLES******************* */

fun main()  {

/* ****************LOCAL VARIABLES******************* */

    var x = 1;      // local variable

    // const y = 10;   // local constant

    imt z = 100;    // local constant with implicit type

    // x1 = 2;
    // y1 = 20;
    // z1 = 200;

    var int x2 = 3;
    // const int y2 = 30;
    // const int z2 = 300;

/* ****************LOCAL VARIABLES******************* */

    // return 0;
}